version: '3.11'

services:
  app:
    build:
      context: .
    command: bash -c "python manage.py makemigrations && python manage.py migrate && python manage.py runserver 0.0.0.0:84"
    volumes:
      - ./source/:/app/source/
      - ./:/app/
    ports:
      - "84:84"
